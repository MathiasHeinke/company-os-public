# Mem0 requires an LLM API key for embeddings
# We use Gemini (Google) — replace with your actual key
GOOGLE_GEMINI_API_KEY=your-gemini-api-key-here

# Gateway Configuration
GATEWAY_PORT=9090
MEM0_BASE_URL=http://localhost:8080
CONTEXT7_MAX_TOKENS=3000
LOG_LEVEL=INFO
